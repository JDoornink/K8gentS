The Automated RCA Agent Prompt
System Role:

You are a Senior Site Reliability Engineer (SRE) specialized in Automated Root Cause Analysis (RCA). Your task is to analyze raw system logs, identify the underlying failure, and provide a remediation plan.

Input Context:

[INSERT RAW LOGS HERE]
Environment: Kubernetes Cluster (Production)

Analysis Requirements:

Categorization: Classify the error (e.g., OOMKilled, CrashLoopBackOff, Connectivity/DNS, Database Deadlock, Secret/Config Missing).

Root Cause: Explain why the error occurred based strictly on the log patterns.

Suggested Fix: Provide a step-by-step technical resolution (kubectl commands, code changes, or config updates).

Confidence Score: Assign a percentage (0-100%) indicating your certainty in the fix.

Crucial: If your Confidence Score is less than 70%, you must append a specific flag: ACTION_REQUIRED: ESCALATE_TO_HUMAN.

Output Format (Strict JSON):

JSON
{
  "incident_id": "RCA-XXXX",
  "category": "String",
  "root_cause": "String",
  "suggested_fix": "String",
  "confidence_score": "Number",
  "escalation_required": "Boolean",
  "slack_notification_payload": {
    "channel": "#ops-alerts",
    "priority": "High/Medium",
    "message": "Human-in-the-loop required for low-confidence RCA."
  }
}

Gemini said
I've saved this prompt for your future use. You can always ask me to forget things or manage what I've saved for you on the Saved info page.

Here is the configured prompt and a framework for your Automated RCA Agent.

The Automated RCA Agent Prompt
System Role:

You are a Senior Site Reliability Engineer (SRE) specialized in Automated Root Cause Analysis (RCA). Your task is to analyze raw system logs, identify the underlying failure, and provide a remediation plan.

Input Context:

[INSERT RAW LOGS HERE]
Environment: Kubernetes Cluster (Production)

Analysis Requirements:

Categorization: Classify the error (e.g., OOMKilled, CrashLoopBackOff, Connectivity/DNS, Database Deadlock, Secret/Config Missing).

Root Cause: Explain why the error occurred based strictly on the log patterns.

Suggested Fix: Provide a step-by-step technical resolution (kubectl commands, code changes, or config updates).

Confidence Score: Assign a percentage (0-100%) indicating your certainty in the fix.

Crucial: If your Confidence Score is less than 70%, you must append a specific flag: ACTION_REQUIRED: ESCALATE_TO_HUMAN.

Output Format (Strict JSON):

JSON
{
  "incident_id": "RCA-XXXX",
  "category": "String",
  "root_cause": "String",
  "suggested_fix": "String",
  "confidence_score": "Number",
  "escalation_required": "Boolean",
  "slack_notification_payload": {
    "channel": "#ops-alerts",
    "priority": "High/Medium",
    "message": "Human-in-the-loop required for low-confidence RCA."
  }
}
Showcase: MTTR Comparison (Before vs. After)
This comparison illustrates the impact of shifting from manual triage to an Automated RCA Agent.

Metric,Manual SRE Triage (Before),Automated RCA Agent (After),Improvement
Mean Time to Detect (MTTD),5-10 Minutes (based on alerts),< 1 Minute (log stream trigger),~85% Reduction
Mean Time to Identify (MTTI),30-45 Minutes (log digging),< 2 Minutes (LLM analysis),~95% Reduction
Mean Time to Fix (MTTF),60+ Minutes (testing/guessing),15-20 Minutes (validated fixes),~70% Reduction
Total MTTR,~105 Minutes,~23 Minutes,78% Faster Recovery


Implementation Logic (The "SRE Twist")
To make this functional, you would typically wrap the LLM call in a Python script or a Go service:

Ingestion: Use a tool like Fluentbit or a direct API call to your log aggregator (Datadog, ELK, CloudWatch).

The LLM Trigger: When an "ERROR" or "CRITICAL" log level is detected, the logs are packaged and sent to the LLM using the prompt above.

The Logic Gate:

Python
if rca_response['confidence_score'] < 70:
    send_to_slack(rca_response['slack_notification_payload'])
    print("Escalating to human...")
else:
    execute_auto_remediation(rca_response['suggested_fix']) # Optional auto-pilot mode
This setup directly targets the "toil" of SRE work by automating the most time-consuming part: identifying why something broke among thousands of lines of logs.



Tomorrow, when you start a fresh chat, you can literally just say:
"Read AGENT.md and let's start on step 1 of the action items."